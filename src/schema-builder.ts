import { ConversionInfo } from "./types";
import graphql, { GraphQLSchema } from 'graphql';

export class RedisSchemaBuilder {

    static buildSchemaFromDTOs(convDTOs: ConversionInfo[]) {
        let schema: GraphQLSchema;

        
    }
}
